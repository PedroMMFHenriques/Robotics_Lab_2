run the program with the command:
python navigation.py

essencial packages:
pip install numpy
pip install scipy
pip install matplotlib
pip install pygame

recommended backend for the matplotlib:
pip install pyqt5